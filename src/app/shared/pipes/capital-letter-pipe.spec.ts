import { CapitalLetterPipe } from './capital-letter-pipe';

describe('CapitalLetterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
